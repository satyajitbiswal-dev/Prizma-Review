from .base import *

# Optionally allow selecting different settings modules via an
# environment variable in the future, e.g. DJANGO_SETTINGS_MODULE
# can reference config.settings.local or config.settings.production.
